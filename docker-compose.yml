version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: bimasoft-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ./init:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    networks:
      - bimasoft-network

  backend:
    image: node:16 # Replace with your preferred Node.js version
    container_name: bimasoft-backend
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
    depends_on:
      - db
    environment:
      DB_HOST: ${POSTGRES_HOST}
      DB_USER: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      DB_NAME: ${POSTGRES_DB}
      DB_PORT: ${POSTGRES_PORT}
    ports:
      - "3000:3000"
    networks:
      - bimasoft-network
    command: npm start  # or your start command for Node.js

networks:
  bimasoft-network:
    driver: bridge
